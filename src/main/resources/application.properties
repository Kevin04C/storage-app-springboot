spring.application.name=stonestorage

# SQL Initialization
spring.sql.init.mode=always

# Database
spring.r2dbc.url=r2dbc:postgresql://localhost:5432/storage_app
spring.r2dbc.username=root
spring.r2dbc.password=1234

# Storage provider: local | s3
storage.type=local
# Ruta absoluta donde se almacenan los archivos (montar como volumen en Docker)
# Usar forward slashes o doble backslash en Windows (ej: D:/storage o D:\\storage)
storage.base-path=D:/storagev2

# SpringDoc OpenAPI
springdoc.swagger-ui.enabled=false
springdoc.api-docs.path=/v3/api-docs